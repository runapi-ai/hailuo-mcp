{
  "servers": {
    "hailuo": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@runapi.ai/hailuo-mcp"
      ],
      "env": {
        "RUNAPI_API_KEY": "${RUNAPI_API_KEY}"
      }
    }
  }
}
